# skywire cli visor pk dnslabel

[← skywire cli visor pk](../README.md)

Convert a public key between its hex and base32 DNS-label forms.

With no argument: prints the local visor's PK as a base32 DNS label.
With one argument: detects the form and prints the other.

  hex (66 chars)   →  base32 (53 chars, DNS-label-safe, used in URLs)
  base32 (53)      →  hex (66, used everywhere else in skywire)

## Usage

```
skywire cli pk dnslabel [hex-or-base32-pk]
```

## Global Flags

```
      --json   print output as JSON
```

---
_Generated by `skywire doc` — do not edit by hand._
