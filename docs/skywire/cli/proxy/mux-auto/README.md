# skywire cli proxy mux-auto

[← skywire cli proxy](../README.md)

Adaptive control loop: read the live per-leg latency (mux-info) and
prune a running proxy's mux toward the preset's intent. The primary route
is always kept.

Presets converge the mux to the K lowest-latency legs:
  fastest    primary + 1 lowest-latency leg
  balanced   primary + 3 lowest-latency legs
  resilient  up to 8 lowest-latency legs (max redundancy)

Each tick PRUNES excess/slowest legs toward the preset, then GROWS the set
back up with fresh disjoint legs (visor-side route-calc + mux-add, honoring
--min-hops) when live legs drop below the target — adaptive failover both
ways. Pair with 'mux-info --watch' in another terminal to see the effect.

Example:
  skywire cli proxy mux-auto fastest --watch 5s
  skywire cli proxy mux-auto resilient --dry-run

## Usage

```
skywire cli proxy mux-auto <fastest|balanced|resilient>
```

## Flags

```
      --dry-run          print the prune/grow decision without acting
      --min-hops int     hop-count floor for legs added when growing toward the preset (>=2 keeps grown legs multihop) (default 2)
  -n, --name string      app to adapt (default "skysocks-client")
      --rg uint16        rg disambiguator: ephemeral src_port from 'mux-info' (only needed when the app has multiple active rg's)
  -w, --watch duration   re-evaluate every interval (e.g. 5s); 0 = decide once and exit
```

## Global Flags

```
  -h, --help              show help menu
      --json              print output as JSON
      --rpc string        RPC server address (env: SKYWIRE_RPC) (default "localhost:3435")
      --timeout int       RPC timeout in seconds (0 = unlimited) (default 30)
      --via dmsg://<pk>   remote visor target — dmsg://<pk> or `skynet://<pk>`
```

---
_Generated by `skywire doc` — do not edit by hand._
