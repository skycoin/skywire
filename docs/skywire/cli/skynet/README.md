# skywire cli skynet

[← skywire cli](../README.md)

Skynet provides port forwarding over the Skywire network.

Client commands connect to remote skynet servers and forward traffic to localhost.
Multiple client instances can run simultaneously with different configurations.
Each instance gets a unique name (e.g., skynet-client-8080, skynet-client-3435).

Server commands (srv) expose local ports over the network.

## Usage

```
skywire cli skynet
```

## Subcommands

- [srv](srv/README.md) — Skynet port forwarding server
- [start](start/README.md) — Start skynet client to connect to a remote server
- [status](status/README.md) — Show skynet client status
- [stop](stop/README.md) — Stop a skynet client instance

## Flags

```
      --rpc string   RPC server address (env: SKYWIRE_RPC) (default "localhost:3435")
```

## Global Flags

```
  -h, --help              show help menu
      --json              print output as JSON
      --timeout int       RPC timeout in seconds (0 = unlimited) (default 30)
      --via dmsg://<pk>   remote visor target — dmsg://<pk> or `skynet://<pk>`
```

---
_Generated by `skywire doc` — do not edit by hand._
