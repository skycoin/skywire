# skywire cli skychat group publish

[← skywire cli skychat group](../README.md)

Add a group or channel to this visor's catalog, so anyone holding this
visor's public key can find it by asking.

Off by default, and admin-only. This is a LOCAL hosting decision, not a
property of the group: it is not gossiped, so it says what THIS visor is
willing to answer questions about. Two admins of the same channel can
legitimately differ on whether they advertise it, and neither can un-list
the other's copy.

## Usage

```
skywire cli skychat group publish <group-id> <on|off>
```

## Global Flags

```
      --addr string       skychat HTTP address (default "127.0.0.1:8001")
  -h, --help              show help menu
      --json              print output as JSON
      --timeout int       RPC timeout in seconds (0 = unlimited) (default 30)
      --via dmsg://<pk>   remote visor target — dmsg://<pk> or `skynet://<pk>`
```

---
_Generated by `skywire doc` — do not edit by hand._
