# skywire cli skychat group demote

[← skywire cli skychat group](../README.md)

Demote PK on the named group.

Any existing admin can run this. The founder (the original group
creator, immutable per Record.OwnerPK) cannot be demoted — that's
the recovery anchor that keeps a group reachable even if every other
admin's visor is offline. Demoting a non-admin PK is a no-op
(idempotent).

## Usage

```
skywire cli skychat group demote <group-id> <pk>
```

## Global Flags

```
      --addr string   skychat HTTP address (default "127.0.0.1:8001")
      --json          print output as JSON
```

---
_Generated by `skywire doc` — do not edit by hand._
