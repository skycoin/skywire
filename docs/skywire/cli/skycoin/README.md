# skywire cli skycoin

[← skywire cli](../README.md)

Manage skycoin-daemon and skycoin-web instances on a running
visor — runtime equivalents of the SKYCOIND_* SKYENV knobs in
'skywire cli config gen'. Multi-instance daemons (one per
fibercoin) are added, configured, and started here without
editing skywire-config.json by hand.

## Usage

```
skywire cli skycoin
```

## Subcommands

- [daemon](daemon/README.md) — skycoin-daemon instances (full nodes)

## Global Flags

```
  -h, --help              show help menu
      --jq string         filter JSON output through a jq/gojq expression (implies --json)
      --json              print output as JSON
      --shape             print the output schema skeleton (zero values, all fields) instead of data
      --timeout int       RPC timeout in seconds (0 = unlimited) (default 30)
      --tui               browse commands and help interactively
      --via dmsg://<pk>   remote visor target — dmsg://<pk> or `skynet://<pk>`
```

---
_Generated by `skywire doc` — do not edit by hand._
