# skywire cli skycoin daemon add

[← skywire cli skycoin daemon](../README.md)

Add a new skycoin-daemon instance to the running visor. The
instance is named "skycoin-daemon-<instance-name>" — pass any
identifier; e.g. 'add aix' creates 'skycoin-daemon-aix'.

Chains the underlying primitives (AddApp + SetAppEnv +
UpdateAppArgBatch + SetAutoStart + StartApp) so the operator gets
a working multi-instance daemon entry in one command.

## Usage

```
skywire cli skycoin daemon add <instance-name>
```

## Flags

```
      --api-sets string     comma-separated --enable-gui-api-sets value (empty = daemon defaults)
      --autostart           set auto_start on the new entry so it starts on visor boot
      --data-dir string     daemon --data-dir override (empty = let the visor's launcher pick a per-instance default)
      --fiber-toml string   FIBER_TOML path (run a fibercoin chain instead of vanilla skycoin)
      --port int            daemon HTTP port (0 = auto-allocate base 6420 + 2*N where N is the existing instance count)
      --start               start the daemon immediately after configuring
```

## Global Flags

```
  -h, --help          show help menu
      --json          print output as JSON
      --timeout int   RPC timeout in seconds (0 = unlimited) (default 30)
```

---
_Generated by `skywire doc` — do not edit by hand._
