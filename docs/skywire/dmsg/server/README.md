# skywire dmsg server

[← skywire dmsg](../README.md)

```
┌┬┐┌┬┐┌─┐┌─┐   ┌─┐┌─┐┬─┐┬  ┬┌─┐┬─┐
	││││││└─┐│ ┬ ─ └─┐├┤ ├┬┘└┐┌┘├┤ ├┬┘
	─┴┘┴ ┴└─┘└─┘   └─┘└─┘┴└─ └┘ └─┘┴└─
DMSG Server - relays DMSG traffic between clients.

HTTP Endpoints:
  GET  /health     Health check

Example:
  skywire dmsg server config gen -o dmsg-config.json
  skywire dmsg server start dmsg-config.json
```

## Usage

```
skywire dmsg server
```

## Subcommands

- [config](config/README.md) — Generate a dmsg-server config
- [dial](dial/README.md) — DMSG Dial network test utility
- [start](start/README.md) — Start Dmsg Server

## Global Flags

```
      --with-kill   force exit after 3 interrupt signals (default true)
```

---
_Generated by `skywire doc` — do not edit by hand._
