# skywire svc se

[← skywire svc](../README.md)

┌─┐┬ ┬   ┌─┐┌┐┌┬  ┬
└─┐│││───├┤ │││└┐┌┘
└─┘└┴┘   └─┘┘└┘ └┘

## Usage

```
skywire svc se
```

## Subcommands

- [dmsg](dmsg/README.md) — Generate config for dmsg-server
- [setup](setup/README.md) — Generate config for setup node
- [visor](visor/README.md) — Generate config for skywire-visor

## Flags

```
  -d, --docker           Environment with dockerized skywire-services
  -l, --local            Environment with skywire-services on localhost
  -n, --network string   Docker network to use
                          (default "SKYNET")
  -p, --public           Environment with public skywire-services
```

---
_Generated by `skywire doc` — do not edit by hand._
