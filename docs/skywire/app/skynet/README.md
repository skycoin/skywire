# skywire app skynet

[← skywire app](../README.md)

skywire port forwarding — pair of server (srv) and client (client) subcommands

## Usage

```
skywire app skynet
```

## Subcommands

- [client](client/README.md) — Run the skynet port-forwarding client (was: `skywire app skynet-client`)
- [srv](srv/README.md) — Run the skynet port-forwarding server (was: `skywire app skynet-srv`)

## Global Flags

```
  -h, --help        show help menu
      --jq string   filter JSON output through a jq/gojq expression (implies --json)
      --json        print output as JSON
      --shape       print the output schema skeleton (zero values, all fields) instead of data
      --tui         browse commands and help interactively
```

---
_Generated by `skywire doc` — do not edit by hand._
