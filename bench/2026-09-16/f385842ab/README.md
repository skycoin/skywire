# f385842ab — mux sets after #4946 (events captured)

References: `../98ff6be85/` (criterion 1). Legs sets: `../4ddf1e432/`. Between
those commits and this one only the client-side tunnel dial (#4945) and the
candidate-route race's first-hop filter (#4946) changed. Sets ran through the
default `skysocks-client` instance on :1080.
