package network

// Conn represents a skywire network connection. It wraps net.Conn
// with other skywire-specific data
type Conn struct {
}

// Listener represents a skywire network listener. It wraps net.Listener
// with other skywire-specific data
type Listener struct {
}
