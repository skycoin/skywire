# skywire cli

Command-line documentation for `skywire cli` lives at
[/docs/skywire/cli/README.md](../../docs/skywire/cli/README.md).

That tree is generated from the live cobra command structure — one
markdown file per command, mirroring the subcommand hierarchy. Run
`skywire doc` from the repo root to regenerate after CLI changes.
